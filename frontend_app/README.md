# frontend_app

Minimal React app that renders a centered greeting with a modern light theme.

## Scripts

- `npm install`
- `npm start` (runs the dev server)
- `npm run build` (builds for production)
